{
  "name": "turbo-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Turbo Upload and Payment Service Monorepo",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "changeset": "changeset",
    "version": "changeset version",
    "release": "changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
